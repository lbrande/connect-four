[package]
name = "bin"
version = "0.1.0"
authors = ["lbrande <lovbra00@gmail.com>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
lib = { path = "../lib" }
rand = "0.7.3"
tokio = { version = "0.2.21", features = ["full"] }